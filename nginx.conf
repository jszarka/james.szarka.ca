server {
    listen 80;
    server_name localhost;

    # The crucial lines for Docker log capture
    access_log /dev/stdout;
    error_log /dev/stderr;

    root /usr/share/nginx/html;
    index index.html;

    location / {
        try_files $uri $uri/ =404;
    }
}
